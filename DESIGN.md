# DESIGN.md - hebEDTF

## 1. סקירה כללית (Overview)
ספריית `hebEDTF` נועדה לגשר בין ניסוחים ותאריכים עבריים לבין תקן **EDTF** (Extended Date/Time Format) ברמות 0 ו-1. 

הספרייה מאפשרת לקבל תאריכים עבריים (כגון תאריך מדויק, חודש עברי, או שנה עברית) וביטויי אי-ודאות בעברית, ולהמירם למחרוזות EDTF תקניות או לאובייקטי EDTF במבנה המקובל ב-`python-edtf`.

---

## 2. ניתוח רכיבים ותשתיות קיימות

### א. תקן EDTF (רמות 0-1)
* **רמה 0 (Level 0):** תמיכה בתקן ISO 8601 הבסיסי:
  * תאריכים מלאים (`YYYY-MM-DD`)
  * חודשים ושנים (`YYYY-MM`, `YYYY`)
  * טווחים/אינטרוולים (`YYYY-MM-DD/YYYY-MM-DD`)
* **רמה 1 (Level 1):** הרחבות בסיסיות:
  * סימני אי-ודאות וקירוב: `?` (אי ודאות), `~` (קירוב), `%` (שניהם)
  * חלקים בלתי מסומנים (Unspecified): תו `X` (למשל `199X` או `2023-XX`)
  * טווחים מורחבים/פתוחים: `../YYYY-MM-DD` או `YYYY-MM-DD/..`

### ב. שילוב עם `python-edtf`
* ב-`python-edtf` קיימת פונקציה `text_to_edtf()` הממירה טקסט בשפה חופשית (באנגלית) למחרוזת EDTF.
* `hebEDTF` תממש פונקציה מקבילה בעברית (`hebrew_to_edtf` / `heb_to_edtf`) ותייצר פלט התואם לפורמט של `python-edtf` וניתן לפרסור על ידו (`parse_edtf`).

### ג. בחירת תשתית ללוח העברי: `pyluach` vs `njdate`
* **`pyluach` (נבחרה):** ספריית פייתון נקי (Pure Python), יציבה, מתוחזקת ב-PyPI, ללא תלויות מורכבות, ומספקת חישובי המרה מדויקים ומהירים בין תאריכים עבריים לגריגוריאניים.
* **`njdate`:** עטיפה מעל `pyluach` המסתמכת על הנדסת רגקסים ספציפית ואינה חבילת PyPI רשמית.
* **החלטה:** להישען על **`pyluach`** כתשתית החישוב האסטרונומי/לוחי המרכזית, ולבנות מעליה את שכבת ה-Parsing והמיפוי ל-EDTF.

---

## 3. הגדרת סקופ (In Scope / Out of Scope)

### ✅ מה בסקופ (In Scope)
1. **המרת תאריך עברי מדויק ל-EDTF:**
   * למשל: `א' בתשרי תשפ"ד` $\rightarrow$ `2023-09-16` (Level 0)
2. **מיפוי שנה עברית לטווח גריגוריאני (Year to Interval):**
   * שנה עברית אינה חופפת לשנה גריגוריאנית יחידה.
   * למשל: `תשפ"ד` $\rightarrow$ `2023-09-16/2024-10-02` (Level 0 Interval)
3. **מיפוי חודש עברי לטווח גריגוריאני (Month to Interval):**
   * למשל: `תשרי תשפ"ד` $\rightarrow$ `2023-09-16/2023-10-15`
4. **זיהוי סמני אי-ודאות וקירוב בעברית (Uncertainty & Approximation):**
   * מילים כמו: "בערך", "סביב", "כנראה", "משוער", "לפני", "אחרי", "?", "~"
   * למשל: `סביב תשפ"ד` $\rightarrow$ `2023-09-16/2024-10-02~` (Level 1)
5. **ממשק Python נקי ופשוט:**
   * פונקציות מרכזיות: `hebrew_to_edtf(text: str) -> str` ומיפוי אובייקטים.

### ❌ מה מחוץ לסקופ (Out of Scope)
1. **מנוע גימטריה כללי:** לא תהיה תמיכה במפענח מילים חופשיות לגימטריה שלא בהקשר תאריכים ושנים מקובלות.
2. **לוחות שנה אחרים:** ללא תמיכה בלוח היג'רי, יוליאני, או לוחות שנה אחרים.
3. **תכונות מתקדמות של EDTF Level 2:** ללא תמיכה ברישום מדעי (Scientific Notation), קבוצות/רשימות תאריכים מורכבות, או מספרי עונות מורכבים בצירופים חלקיים.
4. **עיבוד שפה טבעית פרוזאי מורכב (Full NLP):** הספרייה לא תנתח פסקאות טקסט ארוכות, אלא ביטויי תאריך ממוקדים (Date expressions).

---

## 4. מפת דרכים ראשונית (Roadmap)
* **שלב 0:** מחקר והגדרת סקופ (הושלם - מסמך זה).
* **שלב 1:** בניה ראשונית של הממיר (Hebrew Year / Month / Date $\rightarrow$ EDTF Level 0/1) תוך שימוש ב-`pyluach`.
* **שלב 2:** הוספת מנגנון זיהוי ביטויי קירוב/אי-ודאות בעברית (`~`, `?`, `%`).
* **שלב 3:** בדיקות יחידה (Unit tests) וממשק API סופי.
